# Dockerfile

# MySQL 이미지를 기반으로 이미지를 생성함
FROM mysql:latest

#MySQL 설정
ENV MYSQL_ROOT_PASSWORD="${MYSQL_ROOT_PASSWORD}"
ENV MYSQL_DATABASE="${MYSQL_DATABASE}"
ENV MYSQL_USER="${MYSQL_USER}"
ENV MYSQL_PASSWORD="${MYSQL_PASSWORD}"

#포트 설정 
EXPOSE 3306
